sql.selectUser=SELECT * FROM USERS
sql.getUserById = select * from users where id =?
sql.insertUser=INSERT INTO users (name, email) VALUES (?, ?)
cron.user=0 1 * * * *

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

